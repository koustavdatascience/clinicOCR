# Copy the names below into Vercel Project Settings → Environment Variables.
# This repository file contains no values and is not intended for runtime loading.

# Server-only and Sensitive
NEON_DATABASE_URL=
GEMINI_API_KEY=
CLERK_SECRET_KEY=

# Browser-visible Clerk configuration; this key is intentionally public, but should still be managed in Vercel.
# ClinicOCR maps this Vercel-compatible name to Vite's VITE_CLERK_PUBLISHABLE_KEY at build time.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=

# Server-side configuration
NODE_ENV=production
