# Frontend

This folder contains the React user interface for ClinicOCR. It includes the public landing page, clinician workspace, patient and prescription pages, reusable UI components, and browser-side PDF export.
