import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { AnimatePresence, motion, useReducedMotion } from "framer-motion";
import { ArrowDown, ArrowRight, CheckCircle2, FileScan, Languages, LockKeyhole, ScanLine, ShieldCheck, Sparkles } from "lucide-react";
import { useEffect } from "react";
import { useLocation } from "wouter";

const reveal = { hidden: { opacity: 0, y: 22 }, visible: { opacity: 1, y: 0 } };

export function getLandingMotionPlan(reducedMotion: boolean | null) {
  if (reducedMotion === true) return { mode: "reduced" as const, heroScan: { animate: {}, transition: { duration: 0 } }, aboutEvidence: { animate: {}, transition: { duration: 0 } }, signInRings: { animate: {}, transition: { duration: 0 } } };
  return {
    mode: "active" as const,
    heroScan: { animate: { y: ["-180%", "440%"] }, transition: { duration: 2.25, repeat: Infinity, repeatDelay: 0.65, ease: "easeInOut" as const } },
    aboutEvidence: { animate: { y: [0, -12, 0] }, transition: { duration: 3.2, repeat: Infinity, ease: "easeInOut" as const } },
    signInRings: { animate: { scale: [0.9, 1.18, 0.9], opacity: [0.2, 0.65, 0.2] }, transition: { duration: 3.6, repeat: Infinity, ease: "easeInOut" as const } },
  };
}

function Brand() {
  return <div className="flex items-center gap-3 text-white"><div className="flex h-10 w-10 items-center justify-center rounded-[15px] bg-white text-xl font-bold text-teal-800 shadow-[0_10px_32px_rgba(0,0,0,0.18)]">+</div><div className="leading-none"><p className="font-display text-xl font-bold tracking-[-0.06em]">ClinicOCR</p><p className="mt-1 text-[0.61rem] font-bold uppercase tracking-[0.18em] text-teal-200">Prescription intelligence</p></div></div>;
}

function Label({ index, children }: { index: string; children: React.ReactNode }) {
  return <p className="inline-flex items-center gap-2 text-xs font-bold uppercase tracking-[0.18em]"><span className="inline-flex h-5 min-w-5 items-center justify-center rounded-full bg-current/10 px-1 text-[0.58rem]">{index}</span>{children}</p>;
}

function ReviewPreview({ reducedMotion }: { reducedMotion: boolean | null }) {
  const plan = getLandingMotionPlan(reducedMotion);
  const still = reducedMotion === true;
  return <motion.div initial={{ opacity: 0, y: 22, scale: 0.96 }} animate={{ opacity: 1, y: 0, scale: 1 }} transition={{ duration: still ? 0 : 0.72, delay: 0.18 }} className="relative mx-auto mt-12 w-full max-w-[660px]"><motion.div aria-hidden animate={still ? {} : { scale: [0.94, 1.08, 0.94], opacity: [0.28, 0.72, 0.28] }} transition={{ duration: 3.8, repeat: Infinity, ease: "easeInOut" }} className="absolute -inset-12 rounded-[3.2rem] bg-[radial-gradient(circle_at_45%_35%,rgba(112,234,215,0.42),transparent_58%)] blur-2xl" /><div className="relative overflow-hidden rounded-[2rem] border border-white/20 bg-[#f6fbfa] p-3 shadow-[0_38px_90px_rgba(2,20,31,0.42)]"><div className="flex items-center justify-between rounded-[1.45rem] bg-[#ecf5f3] px-5 py-3"><div className="flex items-center gap-2"><motion.span animate={still ? {} : { scale: [1, 1.65, 1] }} transition={{ repeat: Infinity, duration: 1.5 }} className="h-2 w-2 rounded-full bg-teal-500" /><span className="text-xs font-bold tracking-wide text-teal-900">PRESCRIPTION REVIEW</span></div><span className="rounded-full bg-white px-3 py-1 text-[0.64rem] font-bold uppercase tracking-[0.12em] text-slate-500">Doctor controlled</span></div><div className="grid gap-3 p-3 sm:grid-cols-[0.76fr_1.24fr]"><div className="relative min-h-64 overflow-hidden rounded-2xl border border-slate-200 bg-[linear-gradient(145deg,#fdf8ef,#eee6d7)] p-5"><p className="mt-7 -rotate-2 font-serif text-2xl leading-relaxed text-slate-700">Rx<br /><span className="text-xl">Tab. Paracetamol</span><br /><span className="text-[1.05rem]">দিনে ২ বার</span><br /><span className="text-lg">after food</span></p><div className="absolute bottom-4 left-4 right-4 rounded-lg bg-white/65 px-3 py-2 text-[0.61rem] font-medium text-slate-500">Temporary review source</div><motion.div data-testid="hero-scan-line" aria-hidden animate={plan.heroScan.animate} transition={plan.heroScan.transition} className="absolute inset-x-0 top-0 h-12 bg-[linear-gradient(180deg,transparent,rgba(26,182,173,0.16),rgba(153,255,240,0.85),rgba(26,182,173,0.13),transparent)] shadow-[0_0_18px_rgba(87,237,218,0.75)]" /></div><div className="space-y-3 rounded-2xl bg-white p-5 shadow-[0_10px_24px_rgba(15,70,70,0.06)]"><div className="flex items-center justify-between"><div><p className="text-[0.62rem] font-bold uppercase tracking-[0.16em] text-teal-700">Structured draft</p><p className="mt-1 font-display text-lg font-bold tracking-[-0.04em] text-slate-900">Ready for review</p></div><Sparkles className="h-4 w-4 text-teal-700" /></div><div className="rounded-xl border border-teal-100 bg-teal-50/70 p-3"><p className="text-[0.61rem] font-bold uppercase tracking-[0.14em] text-teal-700">Source language</p><p className="mt-1 text-sm font-bold text-slate-800">Bengali · বাংলা</p></div><div className="space-y-2"><div className="h-2.5 w-[88%] rounded-full bg-teal-100" /><div className="h-2.5 w-[67%] rounded-full bg-slate-100" /><div className="h-2.5 w-[76%] rounded-full bg-slate-100" /></div><div className="flex items-center gap-2 rounded-xl bg-slate-900 px-3 py-2.5 text-xs font-semibold text-white"><CheckCircle2 className="h-3.5 w-3.5 text-teal-300" />Nothing saves without approval.</div></div></div></div><div className="absolute -right-3 top-[20%] hidden rounded-2xl border border-white/30 bg-white/95 px-3 py-2 shadow-xl sm:block"><div className="flex items-center gap-2"><Languages className="h-4 w-4 text-teal-700" /><span className="text-xs font-bold text-slate-700">Language retained</span></div></div></motion.div>;
}

function About({ reducedMotion }: { reducedMotion: boolean | null }) {
  const plan = getLandingMotionPlan(reducedMotion);
  const still = reducedMotion === true;
  const highlights = [{ icon: FileScan, label: "Review source", value: "Temporary by design" }, { icon: Languages, label: "Language", value: "Native script retained" }, { icon: ShieldCheck, label: "Approval", value: "Doctor decides" }];
  return <section id="about" className="relative overflow-hidden bg-[#f2fbf8] px-5 py-24 text-slate-900 lg:px-8 lg:py-32"><div className="relative mx-auto grid max-w-7xl gap-14 lg:grid-cols-[0.84fr_1.16fr] lg:items-center"><motion.div initial={{ opacity: 0, x: still ? 0 : -24 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true, amount: 0.3 }} transition={{ duration: still ? 0 : 0.56 }}><Label index="02">About ClinicOCR</Label><h2 className="mt-5 font-display text-5xl font-bold leading-[0.95] tracking-[-0.07em] text-slate-950">A safer path from ink to insight.</h2><p className="mt-6 max-w-lg text-base leading-7 text-slate-600">ClinicOCR turns a scan into an organized, searchable draft. The upload exists only for OCR and doctor review; the approved clinical record contains reviewed text, not the image.</p><div className="mt-8 grid gap-3">{highlights.map(item => <div key={item.label} className="flex items-center gap-3 rounded-2xl border border-teal-900/8 bg-white/80 px-4 py-3 shadow-[0_10px_24px_rgba(8,79,80,0.04)]"><div className="flex h-9 w-9 items-center justify-center rounded-xl bg-teal-50 text-teal-700"><item.icon className="h-4 w-4" /></div><div><p className="text-[0.62rem] font-bold uppercase tracking-[0.14em] text-teal-700">{item.label}</p><p className="mt-0.5 text-sm font-semibold text-slate-800">{item.value}</p></div></div>)}</div></motion.div><motion.div data-testid="about-evidence-flow" animate={plan.aboutEvidence.animate} transition={plan.aboutEvidence.transition} className="rounded-[2rem] border border-teal-900/10 bg-white p-5 shadow-[0_26px_55px_rgba(8,79,80,0.14)]"><div className="flex items-center justify-between"><div><p className="text-[0.65rem] font-bold uppercase tracking-[0.16em] text-teal-700">Review flow</p><p className="mt-1 font-display text-2xl font-bold tracking-[-0.05em]">One visible handoff</p></div><ScanLine className="h-6 w-6 text-teal-700" /></div><div className="mt-6 grid grid-cols-3 gap-2"><div className="rounded-2xl border border-amber-100 bg-[#fff9ee] p-3"><span className="text-[0.6rem] font-bold uppercase tracking-wide text-amber-700">Upload</span><p className="mt-3 font-serif text-lg text-slate-700">Rx<br />দিনে ২ বার</p></div><div className="flex items-center justify-center"><ArrowRight className="h-5 w-5 text-teal-700" /></div><div className="rounded-2xl bg-slate-950 p-3 text-white"><span className="text-[0.6rem] font-bold uppercase tracking-wide text-teal-300">Approved</span><p className="mt-3 text-sm font-bold leading-5">Text-only<br />record</p></div></div><div className="mt-5 overflow-hidden rounded-full bg-teal-50 p-1"><div className="h-2 w-full rounded-full bg-[linear-gradient(90deg,#0d766f,#82e4d4)]" /></div><div className="mt-4 flex items-center justify-between text-[0.68rem] font-bold uppercase tracking-[0.11em] text-slate-400"><span>Review image</span><span>Doctor review</span><span>Text record</span></div></motion.div></div></section>;
}

function SignIn({ loading, user, onEnter, reducedMotion }: { loading: boolean; user: unknown; onEnter: () => void; reducedMotion: boolean | null }) {
  const plan = getLandingMotionPlan(reducedMotion);
  return <section id="signin" className="relative isolate overflow-hidden bg-[#062c36] px-5 py-24 text-white lg:px-8 lg:py-32"><div className="relative mx-auto grid max-w-6xl gap-10 rounded-[2rem] border border-white/12 bg-white/[0.06] p-7 shadow-[0_32px_80px_rgba(0,0,0,0.25)] backdrop-blur-sm md:grid-cols-[0.94fr_1.06fr] md:p-12"><div><Label index="03">Secure sign in</Label><h2 className="mt-5 font-display text-5xl font-bold leading-[0.96] tracking-[-0.07em]">Your clinical workspace is ready when you are.</h2><p className="mt-6 max-w-md text-base leading-7 text-teal-50/78">Sign in to open the doctor-controlled workspace. Approved records retain structured clinical text; temporary review sources are not kept after approval.</p><Button onClick={onEnter} size="lg" className="mt-8 h-13 rounded-xl bg-white px-6 text-base font-bold text-teal-950 shadow-[0_18px_35px_rgba(0,0,0,0.22)] hover:bg-teal-50">{loading ? "Checking session" : user ? "Open workspace" : "Sign in to ClinicOCR"}<ArrowRight className="ml-2 h-4 w-4" /></Button></div><div className="relative flex min-h-[320px] items-center justify-center overflow-hidden rounded-[1.7rem] border border-white/12 bg-[#073d49] p-6"><motion.div data-testid="signin-session-rings" animate={plan.signInRings.animate} transition={plan.signInRings.transition} className="absolute h-52 w-52 rounded-full border border-teal-300/35" /><div className="relative w-full max-w-xs rounded-2xl bg-white p-5 text-slate-900 shadow-2xl"><div className="flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-teal-50 text-teal-700"><LockKeyhole className="h-5 w-5" /></div><div><p className="font-display text-lg font-bold tracking-[-0.04em]">Protected workspace</p><p className="text-xs text-slate-500">ClinicOCR account access</p></div></div><div className="mt-6 space-y-3">{["Identity verified", "Encrypted session", "Workspace unlocked"].map((step, index) => <div key={step} className="flex items-center gap-3"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-teal-50 text-[0.65rem] font-bold text-teal-800">{index + 1}</span><span className="text-sm font-semibold text-slate-700">{step}</span></div>)}</div><div className="mt-6 flex items-center gap-2 rounded-xl bg-teal-50 px-3 py-2 text-xs font-bold text-teal-800"><ShieldCheck className="h-4 w-4" />Session secured</div></div></div></div></section>;
}

export default function Landing() {
  const [, setLocation] = useLocation();
  const { user, loading, login } = useAuth();
  const reducedMotion = useReducedMotion();
  const preserveLandingPreview = import.meta.env.DEV && new URLSearchParams(window.location.search).has("preview");
  useEffect(() => { if (user && !preserveLandingPreview) setLocation("/workspace"); }, [preserveLandingPreview, setLocation, user]);
  const enterWorkspace = () => { if (user) { setLocation("/workspace"); return; } login(); };
  const scrollTo = (id: string) => {
    if (id === "signin") {
      enterWorkspace();
      return;
    }
    document.getElementById(id)?.scrollIntoView?.({ behavior: reducedMotion ? "auto" : "smooth", block: "start" });
  };
  return <main className="min-h-screen overflow-hidden bg-[#062c36] text-white selection:bg-teal-300 selection:text-teal-950"><section id="hero" className="relative isolate min-h-screen overflow-hidden"><div className="pointer-events-none absolute inset-0 -z-10 bg-[radial-gradient(circle_at_14%_12%,rgba(49,170,164,0.3),transparent_31%),radial-gradient(circle_at_87%_42%,rgba(118,219,206,0.16),transparent_26%),linear-gradient(150deg,#062c36_0%,#073c48_50%,#052b35_100%)]" /><nav className="relative mx-auto flex max-w-7xl items-center justify-between px-5 py-6 lg:px-8"><Brand /><div className="hidden items-center gap-5 text-sm font-semibold text-teal-50/75 md:flex"><button onClick={() => scrollTo("hero")}>Hero</button><button onClick={() => scrollTo("about")}>About</button><button onClick={() => scrollTo("signin")}>Sign in</button></div><Button onClick={() => scrollTo("signin")} variant="outline" className="border-white/20 bg-white/10 text-white hover:bg-white hover:text-teal-950">{loading ? "Checking session" : user ? "Open workspace" : "Sign in"}<ArrowRight className="ml-2 h-4 w-4" /></Button></nav><div className="relative mx-auto max-w-7xl px-5 pb-28 pt-14 lg:px-8 lg:pb-32 lg:pt-20"><motion.div initial="hidden" animate="visible" transition={{ staggerChildren: reducedMotion ? 0 : 0.11 }} className="mx-auto max-w-4xl text-center"><motion.div variants={reveal}><Label index="01">The ClinicOCR Hero</Label></motion.div><motion.h1 variants={reveal} className="mt-7 font-display text-5xl font-bold leading-[0.94] tracking-[-0.075em] text-white sm:text-7xl lg:text-[5.6rem]">The prescription stays human.<br /><span className="bg-[linear-gradient(90deg,#92efe0,#d5fff7,#73d8e0)] bg-clip-text text-transparent">The record becomes clear.</span></motion.h1><motion.p variants={reveal} className="mx-auto mt-7 max-w-2xl text-base leading-7 text-teal-50/80 sm:text-lg">ClinicOCR turns handwritten prescriptions—across languages—into organized, searchable text records while keeping the doctor in control.</motion.p><motion.div variants={reveal} className="mt-9 flex flex-col items-center justify-center gap-3 sm:flex-row"><Button onClick={() => scrollTo("signin")} size="lg" className="h-12 rounded-xl bg-white px-6 text-base font-bold text-teal-950 shadow-[0_18px_35px_rgba(0,0,0,0.22)] hover:bg-teal-50">Enter ClinicOCR<ArrowRight className="ml-2 h-4 w-4" /></Button><Button onClick={() => scrollTo("about")} variant="ghost" className="text-teal-50 hover:bg-white/10 hover:text-white">See how it works<ArrowDown className="ml-2 h-4 w-4" /></Button></motion.div></motion.div><ReviewPreview reducedMotion={reducedMotion} /></div></section><About reducedMotion={reducedMotion} /><SignIn loading={loading} user={user} onEnter={enterWorkspace} reducedMotion={reducedMotion} /><AnimatePresence>{loading && <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: 12 }} className="pointer-events-none fixed bottom-5 left-1/2 z-20 -translate-x-1/2 rounded-full border border-white/15 bg-slate-950/65 px-4 py-2 text-xs font-medium text-white/80 backdrop-blur">Secure session check in progress</motion.div>}</AnimatePresence></main>;
}
